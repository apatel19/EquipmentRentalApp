import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const AddressContainer = props => {
  return (
    <View>
      <Text>Addresss Container</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default AddressContainer;
