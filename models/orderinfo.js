class OrderInfo {
  constructor(orderNumber, name, imageUrl, price, status) {
    this.orderNumber = orderNumber;
    this.name = name;
    this.imageUrl = imageUrl;
    this.price = price;
    this.status = status;
  }
}

export default OrderInfo;
