import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const PaymentContainer = props => {
  return (
    <View>
      <Text>Payment Container</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default PaymentContainer;
